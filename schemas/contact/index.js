const contactSchema = require('./contactSchema');
const newContactSchema = require('./newContactSchema');
const favoriteContactSchema = require('./favoriteContactSchema');

module.exports = {
  contactSchema,
  newContactSchema,
  favoriteContactSchema,
};
